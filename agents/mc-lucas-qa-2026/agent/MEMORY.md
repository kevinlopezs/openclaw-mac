# MEMORY.md — Lucas (Long-term)

- Mantén las pruebas reproducibles. Cada bug necesita un camino a seguir.
- Usa las herramientas de LLM (`Tools`) inyectadas `mobile_mobile_*` (doble prefijo) para instalar apps, sacar capturas e interactuar. **ATENCIÓN: Estas son LLM Tools, NO COMANDOS BASH.** No intentes ejecutarlas usando la tool `exec` o `bash`. Llámalas directamente.
- Nunca describas pasos de usuario (abrir app, escribir en un campo, grabar, subir a Bunny, etc.) si no acabas de ejecutar las tools correspondientes. Si una acción depende de MCP o Shell y esas tools fallan, explica que no pudiste completarla en lugar de afirmar que sí se hizo.
- Para flujos complejos (por ejemplo “abre Manteniclic en ambos emuladores, ejecuta un journey y súbeme los videos a Bunny”), divide siempre el trabajo en pasos: primero usa `mobile_mobile_*` para interactuar con los emuladores, luego los comandos de grabación (iOS/Android), después el uploader a Bunny y finalmente GitHub. No intentes saltarte pasos ni simular acciones que dependen de MCP o Shell.
- En Android: cuando necesites abrir Manteniclic, usa SIEMPRE MCP (`mobile_mobile_launch_app` con `packageName`). No intentes abrirlo con adb como alternativa.
- Nunca uses `exec`, `bash` o comandos de terminal para invocar herramientas `mobile_mobile_*`. Esas tools se invocan solo como MCP tools por su nombre exacto (y si falla con “command not found”, debes corregir el modo de invocación, no intentar adb).
- Si te aparece un mensaje tipo “tool not found / no se encuentra la herramienta mobile*mobile*\*”, nunca inventes alternativas (adb): asume que intentaste un nombre incorrecto y repite usando los **nombres críticos exactos**.
- **Grabar Videos en iOS:** Nunca uses MCP para grabar video en iOS (`mobile_mobile_start_screen_recording`, `mobile_mobile_stop_screen_recording`, etc.). Siempre que el usuario te pida “grabar” o “hacer un video” del simulador, DEBES interpretar esa instrucción como: lanzar `nohup xcrun simctl io booted recordVideo --mask=ignored ~/Desktop/QA_bug_$(date +%Y%m%d_%H%M%S).mp4 > /dev/null 2>&1 & echo $!` vía Shell, guardar el PID y luego detenerlo con `kill -SIGINT <PID>`. Nunca solo `&` porque el proceso morirá al instante en el entorno MCP. Antes de afirmar que el archivo existe, verifica con `ls` que realmente está en el Escritorio y, si no aparece, di que la grabación falló en lugar de inventar una ruta.
- **Grabar Videos en Android:** Para Android, prioriza siempre `adb screenrecord` en lugar de MCP, usando SIEMPRE la ruta absoluta de `adb` para evitar problemas de `PATH`: `/Users/kevinlopez/Library/Android/sdk/platform-tools/adb`. Nunca uses tools MCP de grabación de pantalla de `mobile-next` salvo que el usuario lo pida explícitamente por nombre. Cuando el usuario te pida un video de Android en lenguaje natural, lanza `/Users/kevinlopez/Library/Android/sdk/platform-tools/adb shell screenrecord --time-limit 180 /sdcard/QA_bug_android.mp4` (si hace falta, con `nohup`), luego usa `/Users/kevinlopez/Library/Android/sdk/platform-tools/adb pull /sdcard/QA_bug_android.mp4 ~/Desktop/QA_bug_android_$(date +%Y%m%d_%H%M%S).mp4` y verifica con `ls ~/Desktop/QA_bug_android_*.mp4` antes de afirmar que el archivo existe. Si la verificación falla, dilo explícitamente y no inventes rutas.
- **Subir videos a BunnyCDN:** Después de verificar que el `.mp4` existe en el Escritorio (iOS o Android), usa SIEMPRE la tool MCP de BunnyCDN uploader (por ejemplo `image-uploader_upload_media`) pasando la ruta absoluta del archivo como `file_path`. Usa esa tool como Tool nativa (no como comando Bash). Conserva la URL HTTPS devuelta y úsala en tus reportes; si el upload falla, reporta el error y nunca inventes una URL.
- **Nunca pedir credenciales de Bunny:** aunque el upload falle, no preguntes por Access Key/Storage Zone ni intentes configurar Bunny “a mano”. Reporta el error exacto de la tool MCP y recomienda reintentar.
- En pruebas que involucren un backend o servicios, avisa a Ricardo.
- En problemas de React/Next.js/SEO avisa a Francisco.
- En problemas de Flutter/Dart UI avisa a Maya.
